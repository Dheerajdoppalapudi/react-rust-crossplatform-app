process flow worth giving a shot
---------------------------------------
User Request
      │
      ▼
Prompt Normalizer
      │
      ▼
Cache Check (Redis)
      │
      ├── Found → return image
      │
      ▼
Queue (Kafka)
      │
      ▼
GPU Workers
   Stable Diffusion
      │
      ▼
Upscaler
      │
      ▼
CDN Storage
      │
      ▼
Return URL


Current process flow diagram
User Prompt
    ↓
create_plan() — 1 LLM call
    classifies intent_type, decides frame_count,
    writes frame descriptions + narration + captions
    ↓
Route by intent_type:
    ├── process / architecture / timeline
    │       → generate_mermaid_frames()
    │         LLM writes Mermaid text → Node sidecar → Excalidraw elements
    │
    ├── math
    │       → generate_manim_frames()
    │         LLM writes Python/Manim → subprocess renders → PNG
    │
    └── concept_analogy / comparison / illustration
            → generate_all_frames()
              LLM writes raw slim JSON (x, y, width, height coords) → enhance()
    ↓
combine_frames() — stitch frames side by side
    ↓
enhance() — slim JSON → full .excalidraw format
    ↓
Return .excalidraw + narration text per frame


Drawbacks of illustrations flow:
      LLMs are very bad at spatial reasoning in pixel coordinates. They don't "see" a canvas. They're pattern-matching from training data, not reasoning about space.
      The shape vocabulary is too primitive. Rectangles + ellipses + lines can't make a convincing robot doing a flip, let alone explain thermodynamics with a meaningful visual.
      There's no feedback loop. The LLM generates blind — it can't see the output and fix proportions.
      Thermodynamics-style concept diagrams (molecules, heat flow, pressure) are particularly bad here because they need curves, gradients, and accurate spatial metaphors that require real drawing tools.



Intent Type	Primary Path	Fallback	Output Format
process	Mermaid (Node sidecar)	Slim JSON	.excalidraw JSON
architecture	Mermaid (Node sidecar)	Slim JSON	.excalidraw JSON
timeline	Mermaid (Node sidecar)	Slim JSON	.excalidraw JSON
math	Manim (subprocess render)	Slim JSON	PNG paths
illustration	SVG (cairosvg render)	Slim JSON	PNG paths
concept_analogy	SVG (cairosvg render)	Slim JSON	PNG paths
comparison	SVG (cairosvg render)	Slim JSON	PNG paths

Intent → Renderer routing table:
Intent type	Renderer	Output
math	Manim (--save_last_frame)	PNG
illustration	SVG → cairosvg	PNG
concept_analogy	SVG → cairosvg	PNG
comparison	SVG → cairosvg	PNG
process	Mermaid (mmdc)	PNG
architecture	Mermaid (mmdc)	PNG
timeline	Mermaid (mmdc)	PNG
anything else	Slim JSON	.excalidraw




If TTS fails for a frame, fall back to a duration estimate: word_count × 0.45 seconds (average speaking pace)
If a frame PNG is None (render failed), show a placeholder "Frame unavailable" card instead of crashing
If ffmpeg isn't installed, detect early and give a clear error



User Prompt
    │
    ▼
┌─────────────────────────────────────────┐
│  POST /api/generate  (main.py)          │
│  PLANNER LLM call                       │
│  → intent_type, N frames,               │
│    shared_style, element_vocabulary     │
└─────────────────┬───────────────────────┘
                  │
        ┌─────────▼──────────┐
        │  Route by intent   │
        └──┬──────┬──────┬───┘
           │      │      │
         math  illust/ process/
               concept/ arch/
               compar. timeline
                       + fallback
           ▼      ▼      ▼
      ┌────────┐ ┌────┐ ┌────────┐ ┌──────────┐
      │ Manim  │ │SVG │ │Mermaid │ │ Slim JSON│
      │        │ │    │ │        │ │          │
      │LLM→.py │ │LLM→│ │LLM→    │ │LLM→      │
      │        │ │SVG │ │.mmd    │ │.excali-  │
      │manim   │ │    │ │        │ │draw JSON │
      │render  │ │cairo│ │mmdc   │ │          │
      │ -ql    │ │svg→│ │CLI→    │ │          │
      │        │ │PNG │ │PNG     │ │          │
      └───┬────┘ └─┬──┘ └───┬────┘ └────┬─────┘
          │        │        │            │
        .mp4      PNG      PNG        null PNG
    (animation)            │      (placeholder
          │        │       │       card later)
          └────────┴───────┘
                   │
                   ▼
           frames.json saved
           {path, caption} × N
           (path is .mp4 for Manim,
            .png for all others)
                   │
    ───────────────────────────────
    POST /api/generate_video/{id}
    ───────────────────────────────
                   │
          ┌────────▼─────────┐
          │  frame_exporter  │
          │                  │
          │ PNG → letterbox  │
          │ 1920×1080        │
          │ + subtitle burn  │
          │                  │
          │ MP4 → passed     │
          │ through as-is    │
          └────────┬─────────┘
                   │
          ┌────────▼─────────┐
          │  tts_service     │
          │                  │
          │ narration.txt    │
          │ → per-frame text │
          │ gTTS → .mp3      │
          │ (parallel)       │
          └────────┬─────────┘
                   │
          ┌────────▼─────────────────────────┐
          │  video_assembler                  │
          │                                   │
          │  per frame:                       │
          │  ┌──────────┬────────────────┐    │
          │  │  .png    │     .mp4       │    │
          │  │          │  (Manim)       │    │
          │  │ImageClip │VideoFileClip   │    │
          │  │Ken Burns │plays animation │    │
          │  │1.0→1.02x │                │    │
          │  │          │ if audio >     │    │
          │  │          │ animation:     │    │
          │  │          │ freeze last    │    │
          │  │          │ frame to fill  │    │
          │  └────┬─────┴───────┬────────┘    │
          │       └──────┬──────┘             │
          │              │ attach TTS audio   │
          │              │ crossfade 0.4s     │
          │              ▼                    │
          │      concatenate all clips        │
          │      export H.264 + AAC .mp4      │
          └──────────────┬────────────────────┘
                         │
                         ▼
              video_path saved to DB
                         │
                         ▼
         GET /api/sessions/{id}/video
         → FileResponse streams .mp4
