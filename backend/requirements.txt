# Web framework
fastapi>=0.115
uvicorn[standard]>=0.30

# Configuration & environment
# Upper bound required by manim-voiceover (pins <0.22). Only load_dotenv is used,
# whose API is stable across 0.21/1.x. When the video worker is split into its own
# deployable, move manim-voiceover there and this pin can return to >=1.0.
python-dotenv>=0.21,<0.22
pydantic>=2.0

# Form / file upload support
python-multipart>=0.0.9

# LLM providers
openai>=1.0
anthropic>=0.25

# Frame generation
manim
manim-voiceover[gtts]   # narrated VoiceoverScene beats — audio synced to animation at render time
cairosvg
lxml
requests
playwright

# Video pipeline
moviepy
imageio-ffmpeg
Pillow>=10.0

# Text-to-speech
gtts

# Auth
google-auth>=2.0
PyJWT>=2.8
bcrypt>=4.0
email-validator>=2.0

# Rate limiting
slowapi>=0.1.9

# Deep research
tavily-python>=0.3.0
pdfplumber>=0.10.0
python-pptx>=0.6.21
pgvector>=0.3.0   # vector type codec for asyncpg; embeddings live in Postgres (RDS) now

# Observability
structlog>=24.0
prometheus-client>=0.20.0

# PostgreSQL — async driver for app code
asyncpg>=0.29

# Migrations (asyncpg used by both app and Alembic — no psycopg2 anywhere)
alembic>=1.13
sqlalchemy[asyncio]>=2.0  # asyncio extra pulls in greenlet, required for run_sync

# AWS S3
boto3>=1.34

# Testing
pytest>=7.0
pytest-asyncio>=0.23
