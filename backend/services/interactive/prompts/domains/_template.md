## {Domain Name} Domain Guidance

This question is from {domain}. Apply these preferences when selecting entities:

### Entity preferences

- **{Topic category 1}** ({examples}):
  - Preferred entity type: `{type}` because ...
  - Include `{prop}` in props: ...

- **{Topic category 2}** ({examples}):
  - Preferred entity type: `{type}` because ...

### Layout guidance

- {Which entity goes first and why}
- {How many entities to use}

### Notes

- {Any domain-specific LLM instructions, e.g. units, notation, conventions}
